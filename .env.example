# Copy to .env if you need to override defaults for local (non-Docker) runs.
# Milestone 1 (Project Foundation) scope -- only what the foundation needs.
# JWT/auth secrets and AI provider keys are added in the milestones that
# introduce those features.

# apps/api
DATABASE_URL=postgresql://knowledgehub:knowledgehub@localhost:5432/knowledgehub
QDRANT_URL=http://localhost:6333
WEB_ORIGIN=http://localhost:3000

# apps/web
NEXT_PUBLIC_API_URL=http://localhost:8000
