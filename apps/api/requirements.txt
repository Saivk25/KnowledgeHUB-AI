# Milestone 1 (Project Foundation) dependencies only.
# PyMuPDF, passlib, python-jose, python-multipart, and email-validator are
# used by later milestones (ingestion, auth) and are intentionally not
# installed yet -- they will be added back to this file in the milestone
# that introduces the feature that needs them.
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
qdrant-client==1.11.1
python-dotenv==1.0.1
